from data.assignment_tests.example2 import *
import pytest
from typing import *

# Test cases to achieve 100% code coverage

def test_initial_conditions():
    testme(0, 10, 50)  # a < b, c in range
    testme(0, 10, 200)  # a < b, c in range
    testme(0, 10, 100)  # a < b, c in range
    testme(0, 10, 1)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range
    testme(0, 10, 571)  # a < b, c not in range
    testme(0, 10, 285)  # a < b, c not in range
    testme(0, 10, 57)  # a < b, c not in range
    testme(0, 10, 284)  # a < b, c not in range
    testme(0, 10, 0)  # a < b, c not in range
    testme(0, 10, 570)  # a < b, c not in range
    testme(0, 10, -10)  # a < b, c not in range
    testme(0, 10, 1000)  # a < b, c not in range
    testme(0, 10, -1)  # a < b, c not in range